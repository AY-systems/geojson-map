{
  "name": "geojson-map",
  "version": "1.0.0",
  "description": "CSVから市区町村のリストを取得し、マップを塗りつぶすツール",
  "type": "module",
  "scripts": {
    "build:pmtiles": "./scripts/build-pmtiles.sh",
    "generate:search": "node scripts/generate-search-data.cjs",
    "preprocess": "npm run generate:search",
    "dev": "vite",
    "build": "npm run preprocess && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1",
    "papaparse": "^5.5.3",
    "pmtiles": "^3.2.1"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "vite-plugin-static-copy": "^3.1.4"
  }
}
